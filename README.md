# kudadonbe.github.io

Personal portfolio website built with Vue + Vite and deployed to GitHub Pages.

## Development

```bash
npm install
npm run dev
```

## Deployment

```bash
npm run build
git add .
git commit -m "Deploy updates"
git push
```

The site is automatically served from the `docs/` folder on the `main` branch.
